# Docker-Umgebungsvariablen – lokale Überschreibungen hier eintragen
# Diese Datei NICHT in Git committen wenn echte Passwörter enthalten sind.

# Datenbank
POSTGRES_USER=cooking
POSTGRES_PASSWORD=secret
POSTGRES_DB=cooking_recipes

# Port-Mappings (falls Standardports bereits belegt sind)
HTTP_PORT=8090
POSTGRES_PORT=5433
MAILPIT_HTTP_PORT=8025
MAILPIT_SMTP_PORT=1025
